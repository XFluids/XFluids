#include "inishape.h"

// IniShape::IniShape(sycl::queue &q, size_t num_Box, size_t num_Bubble) : num_box(num_Box), num_bubble(num_Bubble)
// {
// 	iboxs = sycl::malloc_shared<IniBox>(num_box, q);
// 	ibubbles = sycl::malloc_shared<IniBubble>(num_box, q);
// 	// TODO: add boxs and bubble setting in .json file and get them here
// }